input {
  beats {
    port => 5044
  }
}
output {
  if [fields][type] == "mongodb" {
    redis {
      data_type => list
      key => mongodb
      congestion_threshold => 100000
    }
  }
}
