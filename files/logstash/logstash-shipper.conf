input {
  beats {
    port => 5044
  }
}
output {
  if [fields][type] == "dummy" {
    redis {
      data_type => list
      key => dummy
      congestion_threshold => 100000
    }
  }
  if [fields][type] == "apache" {
    redis {
      data_type => list
      key => apache
      congestion_threshold => 100000
    }
  }
}
